/*
========================================
SCRIPT.JS — INTERACTION & UX FILE
========================================

GOAL:
This file handles OPTIONAL JavaScript used to
improve UX and interactivity.

JavaScript is NOT required for visual design,
but it CAN improve usability.

----------------------------------------
RULES
----------------------------------------
- Do NOT manipulate layout with JS
- Do NOT rely on JS for basic navigation
- JS should enhance UX, not replace it
*/

/*
----------------------------------------
EXAMPLE: CONFIRM JS IS CONNECTED
----------------------------------------
Open the browser console.
If you see this message, your JS is working.
*/
console.log("Portfolio JavaScript loaded");

/*
----------------------------------------
OPTIONAL UX ENHANCEMENTS (CHOOSE ONE)
----------------------------------------

You may implement:
- Smooth scrolling
- Button click feedback
- Theme toggle (light/dark)
- Simple hover or focus states
- Navigation highlighting

You do NOT need to implement all of these.
Quality > quantity.
*/

/*
// EXAMPLE: Smooth scrolling for nav links
document.querySelectorAll("nav a").forEach(link => {
  link.addEventListener("click", event => {
    event.preventDefault();
    document
      .querySelector(link.getAttribute("href"))
      .scrollIntoView({ behavior: "smooth" });
  });
});
*/

/*
----------------------------------------
UX CHECK
----------------------------------------
Ask yourself:
- Does this interaction help the user?
- Is it obvious what changed?
- Would the site still work without JS?
*/
